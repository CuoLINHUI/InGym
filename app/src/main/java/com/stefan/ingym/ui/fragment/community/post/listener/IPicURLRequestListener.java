package com.stefan.ingym.ui.fragment.community.post.listener;

/**
 * @ClassName: ${type_name}
 * @Description: 监听上传图片成功之后返回的图片URL路径
 * @Author Stefan
 * @Date ${date} ${time}
 */
public interface IPicURLRequestListener {
    void loadSuccess(Object object);
}
